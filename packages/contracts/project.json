{
  "name": "contracts",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/contracts/src",
  "projectType": "library",
  "targets": {
    "build": { "command": "pnpm -C packages/contracts build", "outputs": ["{projectRoot}/dist"] },
    "lint": { "command": "pnpm -C packages/contracts lint" },
    "typecheck": { "command": "pnpm -C packages/contracts typecheck" },
    "dev": { "command": "pnpm -C packages/contracts dev" }
  }
}
